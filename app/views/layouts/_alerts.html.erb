<div class="col-lg-12" id="alerts">
<% flash.each do |key, value| %>
  <div class="alert alert-<%= twitterized_type(key)%>">
    <a href="#" data-dismiss="alert" class="close">×</a>
      <ul>
        <li>
          <%= value %>
        </li>
      </ul>
  </div>
<% end %>
</div>

